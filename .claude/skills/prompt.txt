# DIRETRIZES DO SISTEMA (NÃO ALTERAR)
1. Inicie lendo o arquivo C:\claude-qagrotis\.claude\skills\orchestrator.md e assuma a persona do Orquestrador.
2. Siga o pipeline rigorosamente: UX → QA (Pré) → Back-end → Front-end → QA (Pós) → Reviewer.
3. [Regra de Ouro]: NUNCA execute todas as etapas de uma vez. Termine uma etapa, apresente o resultado e pergunte se pode prosseguir.
4. Zero Hardcode (Design System) e Documentação obrigatória no Storybook.

# CONTEXTO DA TAREFA
Antes de codificar, utilize suas ferramentas de busca para localizar os arquivos envolvidos:
- src/core/rbac/policy.ts
- src/app/(protected)/LayoutClient.tsx
- src/app/(protected)/configuracoes/JiraConfigButton.tsx
- src/app/(protected)/individual/progressao/page.tsx
- src/hooks/useNotifications.ts (e ações mutáveis relacionadas)
- src/app/(protected)/equipe/* (e suas respectivas abas atuais)
- C:\claude-qagrotis\.claude\skills\front-end.md

# SOLICITAÇÕES

[Segurança e Configurações]
1. RBAC (`policy.ts`): Remover "config.modelosIA" e "config.jira" de todos os papéis (como QA, UX, TW), mantendo-as exclusivas para Administrador:MGR.
2. Navegação Global: Remover permanentemente o menu "Documentos" do array estático NAV_ITEMS e a capability "menu.documentos" de todos os perfis.
3. Desabilitar Gerador (`LayoutClient.tsx`): Usar a propriedade `integracoesProp` injetada. Se a lista de integrações for vazia, marcar o menu "Gerador" como `disabled` para todos no sistema.

[UI e UX]
4. Configuração Jira (`JiraConfigButton.tsx`): Remover o parágrafo "Suas credenciais ficam vinculadas à sua conta..." da modal.
5. Regra de Botões (`front-end.md`): Atualizar a skill de front-end adicionando a regra obrigatória de que "Todos os botões criados/refatorados DEVEM possuir um ícone LucideReact à esquerda do texto". Fazer varredura na aplicação para adicionar ícones faltantes (ex: modal de "Ativar").
6. Ocultar Valores Monetários (Página de Progressão): Inicializar células com valores ofuscados (ex: R$ •••••••). Inserir um <button> minimalista com ícone <Eye> / <EyeOff> ao lado para revelar o valor real apenas ao clicar.

[Features Maiores]
7. Dashboard Progressão MGR (`individual/progressao/page.tsx`): 
   - Renderização condicional para Perfil MGR ou T-AMG.
   - Quando for MGR, exibir um Dashboard com: Topcards (Perfis de Acesso, Usuários, Feedbacks, Avaliações), Tabela "Avaliações" (últimas 4/5, filtráveis por Perfil e data), Gráfico "Usuários por Perfil" e Cards Menores (Avaliações, Feedbacks, Chapters, Ausências).
   - Ocultar cards "Cenários por Módulo" e "Últimas automações" para MGR.
8. Reestruturação do Menu Equipe (`LayoutClient.tsx` e rotas):
   - Criar `EquipeSidebarNavGroup` como menu Collapsible englobando: Performance, Chapters, Horários, Férias, Ausências, Metas e Aniversários.
   - Migrar as <Tabs> da rota `/equipe` para sub-rotas isoladas no Next.js App Router (ex: `/equipe/performance`), com layouts que preservem a navegação e carreguem rápido de forma isolada.

[Performance e Real-Time]
9. Notificações Delay: Eliminar o delay ao salvar avaliações/feedbacks. Atualizar as Server Actions para forçar imediatamente um `revalidateTag('notifications')` ou engatilhar refetch no React Query para que a sineta notifique o cliente no exato momento da inserção no banco.